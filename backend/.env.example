# Copy to .env and fill values for local development
# PostgreSQL example: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/arrival_platform"

# JWT secret for signing tokens
JWT_SECRET="replace_this_with_a_secure_random_string"

# Optional: PORT for backend server
PORT=3000
